services:
  db:
    image: postgres:16
    container_name: capstone_pg
    environment:
      POSTGRES_USER: capstone
      POSTGRES_PASSWORD: capstone_pw
      POSTGRES_DB: capstone_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: